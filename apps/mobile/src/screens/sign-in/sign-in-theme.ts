/** Layout and motion tokens for the sign-in / landing screen. */
export const signInTheme = {
  contentPaddingH: 24,
  heroMaxWidth: 320,
  /** Space between hero headline and subtitle (~0.25× title cap height; tight but readable). */
  heroTitleSubtitleGap: 10,
  ctaMaxWidth: 360,
  titleSize: 34,
  subtitleSize: 16,
  footnoteSize: 12,
  wordmarkSize: 11,
  enterDurationMs: 780,
  enterTranslateY: 10,
} as const;
