{
  "name": "@opensocial/protocol-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @opensocial/protocol-types build && pnpm --filter @opensocial/protocol-events build",
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @opensocial/protocol-types build && pnpm --filter @opensocial/protocol-events build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "pretest": "pnpm --filter @opensocial/protocol-types build && pnpm --filter @opensocial/protocol-events build",
    "test": "echo 'no tests yet for protocol-client package'"
  },
  "dependencies": {
    "@opensocial/protocol-events": "workspace:*",
    "@opensocial/protocol-types": "workspace:*"
  }
}
